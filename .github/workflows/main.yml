name: NTXA Auto Signer

on:
  workflow_dispatch:
    inputs:
      ipa_url:
        description: 'رابط الـ IPA'
        required: true

jobs:
  sign:
    runs-on: macos-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install zsign
        run: brew install zsign

      - name: Process App
        run: |
          # تحميل الملف
          curl -L "${{ github.event.inputs.ipa_url }}" -o original.ipa
          
          # التوقيع
          zsign -k cert.p12 -p "${{ secrets.P12_PASSWORD }}" -m profile.mobileprovision -o signed_ntxa.ipa original.ipa

      - name: Release
        uses: softprops/action-gh-release@v2
        with:
          files: signed_ntxa.ipa
          tag_name: v${{ github.run_number }}
          name: "NTXA Signed Release #${{ github.run_number }}"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Get Link
        run: |
          IPA_RAW="https://github.com/${{ github.repository }}/releases/download/v${{ github.run_number }}/signed_ntxa.ipa"
          echo "--------------------------------------------------"
          echo "انسخ الرابط التالي وافتحه في Safari للتثبيت المباشر:"
          echo "itms-services://?action=download-manifest&url=https://plist-service.vercel.app/plist?url=$IPA_RAW"
          echo "--------------------------------------------------"
